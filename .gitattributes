# Never let Git rewrite line endings on checkout. Release verification hashes exact file bytes, so a CRLF/LF
# conversion (for example a Windows checkout with core.autocrlf=true) would change a file's hash and make a
# legitimate copy fail verification. Keeping every file byte-identical across platforms preserves the signature.
* -text
