# Never commit real secrets. This directory holds the relay token file that the compose services read. Ignore
# everything here except this rule, so a token placed here cannot be committed by accident.
*
!.gitignore
